git add origin <link>
git add <new file>
git pull 
git push
git status 	//check untracked files
git remote -v	//check origin or remote repo
git config --global user.name "FIRST_NAME LAST_NAME"
git config --global user.email "MY_NAME@example.com"
git branch <branchname>
git checkout <branchname> //switch to other branch
git merge <branchname> //merge branch to current checkout
clear
ls //List contents of the current directory

git config --global core.pager cat	//stop displaying log to pager
clear
ls //List contents of the current directory
